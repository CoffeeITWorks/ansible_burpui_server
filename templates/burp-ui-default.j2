# Defaults for burp initscript
# sourced by /etc/init.d/burp-restorer
# installed at /etc/default/burp-restorer by the maintainer scripts

#
# This is a POSIX shell fragment
#

RUN=yes

# Additional options that are passed to the Daemon.
DAEMON_ARGS="-c /etc/burp/burp-ui.conf"
